services:
  tennis-ranks:
    build: .
    container_name: tennis-ranks
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      TENNIS_ADMIN_PASSWORD: "请改成强密码"
      TENNIS_DB_FILE: "/data/tennis.db"
      PORT: "3000"
    volumes:
      - ./data:/data
